/**
 * This is a function that needs extensive documentation
 *
 * @param {number} num - a number
 * @returns {boolean} whether or not the number is even
 */
function isEven(num) {
  return !(num % 2);
}